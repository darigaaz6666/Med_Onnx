py -3.12 -m venv .venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
.\.venv\Scripts\Activate.ps1



# 1) Training (salva best.pt + scaler.pkl in models\staging)
python -m src.train --epochs 25

# 2) Export ONNX FP32
python -m src.export_onnx --in models/staging/best.pt --out models/model_fp32.onnx

# 3) Quantizzazione a INT8
python -m src.quantize_onnx --in models/model_fp32.onnx --out models/model_int8.onnx

# 4) Valutazione FP32 vs INT8 sullo stesso test set
python -m src.evaluate_onnx